// Array Method in map

const numbers = [1, 2, 3, 4];

const newList = numbers.map((el) => console.log(el));

const person = [
  {
    name: "ajith",
    age: 20,
  },
  {
    name: "Sreejith",
    age: 18,
  },
];

person.map((el) => console.log(el.name));
